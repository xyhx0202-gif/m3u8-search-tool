# Cloudflare Pages 路由规则

# 静态文件直接访问，不需要重定向
# Cloudflare Pages会自动处理静态文件

# API请求路径保留原样
/api/* /api/:splat 200
/v1/* /v1/:splat 200

# 针对index_simple的特定路由
/index_simple /index_simple.html 301

# 其他所有请求重定向到index_simple.html
/* /index_simple.html 200
